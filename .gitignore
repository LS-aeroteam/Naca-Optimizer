# Python bytecode
__pycache__/
*.py[cod]

# Virtual environments
.venv/
venv/

# XFOIL executables (GPL binary: downloaded automatically on Windows,
# installed by the user on Linux/macOS) go in naca_core/bin/
/naca_core/bin/
xfoil.exe

# Run outputs: every run creates its own folder under Results/. They are not versioned.
Results/
